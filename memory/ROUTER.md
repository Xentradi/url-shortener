# Memory Router

Read this repo-local router first. Then load only the smallest relevant slice.

- Use `memory/facts/overview.md` for stable project facts.
- Use `memory/open-loops.md` for unresolved items and foot-guns.
- Keep memory thin; docs and code remain the source of truth.
